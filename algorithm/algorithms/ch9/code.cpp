#include <iostream>
int randomized_select(int[], int);
int randomized_partition(int[], int, int);

int select(int[], int);
int main(){

    return 0;
}

int randomized_select(int A[], int p, int r, int i){
    if (p == r){
        return A[p];
    }
    int q = 1;
        return 1;
}

int randomized_partition(int A[], int p, int r){
    return 1;
}

int select(int A[], int i){
    /* 
    1. 划分, 5个一组
    2. 对于所有的元素进行划分,找到中位数
    3. 在中位数中间找到中位数
    4. 以该中位数划分 输入的数组, 得到 低区 和 高区
    5. 在低区 或者 高区递归
    */
    return 1;
}
